"use client"; import {useEffect,useState} from "react"; import {useParams} from "next/navigation"; import {api} from "../../../../lib/api"; import type {Escalation} from "../../../../lib/types"; import {ErrorState,Loading} from "../../../../components/ui";
export default function EscalationDetail(){const {id}=useParams<{id:string}>();const [item,setItem]=useState<Escalation>();const [error,setError]=useState("");useEffect(()=>{api.escalation(id).then(setItem).catch(e=>setError(e.message))},[id]);if(error)return <div className="mx-auto max-w-3xl px-6 py-16"><ErrorState message={error}/></div>;if(!item)return <Loading/>;return <div className="mx-auto max-w-3xl px-6 py-16"><p className="text-sm text-teal">Escalation #{item.id}</p><h1 className="mt-3 text-4xl font-semibold">{item.reason}</h1><span className="mt-5 inline-block rounded-full bg-amber-100 px-3 py-1 text-xs uppercase text-amber-800">{item.status}</span><div className="mt-8 rounded-2xl bg-white p-6 shadow-soft"><p className="whitespace-pre-wrap leading-7 text-ink/75">{item.message}</p><p className="mt-6 text-sm text-ink/50">Conversation: {item.conversation_id}</p></div></div>}
