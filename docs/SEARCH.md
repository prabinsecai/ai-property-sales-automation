# Search and retrieval

Structured search supports city, suburb, property type, price, bedroom, exact bathroom,
parking, pet policy, and availability filters. Natural search accepts plain language and
uses the deterministic local ranker. Reindex creates embeddings for every property in the
Chroma collection; production embeddings can be switched to an OpenAI-compatible provider
with environment settings.

