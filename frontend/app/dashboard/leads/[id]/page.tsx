"use client"; import {useEffect,useState} from "react"; import {useParams} from "next/navigation"; import {api} from "../../../../lib/api"; import type {Lead} from "../../../../lib/types"; import {ErrorState,Loading} from "../../../../components/ui";
export default function LeadDetail(){const {id}=useParams<{id:string}>();const [item,setItem]=useState<Lead>();const [error,setError]=useState("");useEffect(()=>{api.lead(id).then(setItem).catch(e=>setError(e.message))},[id]);if(error)return <div className="mx-auto max-w-3xl px-6 py-16"><ErrorState message={error}/></div>;if(!item)return <Loading/>;return <div className="mx-auto max-w-3xl px-6 py-16"><p className="text-sm text-teal">Lead #{item.id}</p><h1 className="mt-3 text-4xl font-semibold">{item.name||"Unnamed lead"}</h1><div className="mt-8 grid gap-4 rounded-2xl bg-white p-6 shadow-soft sm:grid-cols-2"><p><span className="text-sm text-ink/50">Email</span><br/>{item.email||"—"}</p><p><span className="text-sm text-ink/50">Phone</span><br/>{item.phone||"—"}</p><p><span className="text-sm text-ink/50">Intent</span><br/>{item.intent||"—"}</p><p><span className="text-sm text-ink/50">Score</span><br/>{item.score}/100</p></div><h2 className="mt-8 text-xl font-semibold">Score signals</h2><ul className="mt-3 list-inside list-disc text-ink/65">{item.score_reasons.map(x=><li key={x}>{x}</li>)}</ul></div>}
